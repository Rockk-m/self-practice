ความรู้ที่ได้

1. .filter()
เอาไว้ คัดของออกจาก array ตามเงื่อนไขที่เราสนใจ

2. .map()
เอาไว้ แปลงข้อมูลใน array เป็นรูปแบบใหม่
เช่น “เอาชื่อทั้งหมด แต่เปลี่ยนเป็นตัวพิมพ์ใหญ่”

3. .reduce()
เอาไว้ สรุปค่า จาก array ทั้งหมดให้เหลือแค่ค่าเดียว
เช่น “รวมราคาของสินค้าที่แพง ๆ ทั้งหมด”
